import React from "react";
import "../styles/components.scss";

const ParkingMap = () => {
  return (
    <div className="parking-map">
      <h2>Карта паркування</h2>
      <p>Оберіть місце для паркування.</p>
      {/* Тут буде інтеграція з картою */}
    </div>
  );
};

export default ParkingMap;